pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh "npm install"
                sh "npm run start:dev; done &"
            }
        }
          stage('Test') {
            steps {
                sh "curl https://localhost:3000"
            }
        }
    }
}
