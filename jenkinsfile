pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh "npm install"
                BUILD_ID=dontKillMe nohup bash npm run build.sh &
            }
        }
          stage('Test') {
            steps {
                sh "curl https://localhost:3000"
            }
        }
    }
}
